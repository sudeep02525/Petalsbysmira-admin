/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  experimental: {
    turbopack: {
      root: 'C:/Petals-by-smira',
    },
  },
};

export default nextConfig;
